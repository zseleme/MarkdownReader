name = "mreader"
main = "worker.js"
compatibility_date = "2026-04-14"

[assets]
directory = "."
binding = "ASSETS"

# KV namespace binding is configured in the Cloudflare dashboard:
# Workers & Pages → mreader → Settings → Bindings
# Variable name: DOCUMENTS  |  Type: KV Namespace
